sudo: true
language: python
python:
  - "3.7"
services:
  - docker
before_install:
  - pip install -U pip
  - pip install --user -r requirements.txt
before_script:
  - flake8 src/
script:
  - pytest
deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: develop
